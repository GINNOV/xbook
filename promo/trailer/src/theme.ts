/** XBook console palette (aligned with design.md). */
export const colors = {
  bg: "#0c0e10",
  surface: "#14181c",
  surfaceRaised: "#1a1f24",
  border: "#2a3138",
  text: "#f4f6f8",
  textMuted: "#9aa3ad",
  textDim: "#6b737c",
  accent: "#22c55e",
  accentSoft: "rgba(34, 197, 94, 0.15)",
  xBlue: "#1d9bf0",
  ytRed: "#ff0033",
  warn: "#f59e0b",
};

export const fonts = {
  sans: 'ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif',
  mono: 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace',
};

/** 1080p / 30fps product trailer. */
export const VIDEO = {
  width: 1920,
  height: 1080,
  fps: 30,
} as const;
